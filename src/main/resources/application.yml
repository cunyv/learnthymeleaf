server:
  port: 8080

# thymeleaf
spring:
  thymeleaf:
    prefix: classpath:/template/
    check-template-location: true
    suffix: .html
    encoding: UTF-8
    content-type: text/html
    mode: HTML5
    cache: false